import { LoginClient } from "@/components/login-client";

export const metadata = { title: "Sign In | Savians Tax Assessment Portal" };

export default function LoginPage() { return <LoginClient />; }
